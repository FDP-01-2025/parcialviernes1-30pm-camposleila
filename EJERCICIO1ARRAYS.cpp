// Dado un array, cuenta cuantos elementos aparecen más de una vez.

#include <iostream>
using namespace std;

int main()
{
    int numero, suma, i; //declarar variables

    cout << "inserta los 5 numeros para contar los que se repiten" << endl; 

    for (int i = 1; i <= 5; i++) //Para que el usuario ingrese los numeros (en este caso 5) 
    {
        cout << "Numero " << i << ": ";
        cin >> numero;
    }
    if (numero = numero) 
    {
        cout << numero << " se repite " << i << "veces" << endl;
    }
    
    return 0;
}